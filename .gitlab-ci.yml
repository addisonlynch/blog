image: ruby:2.6

stages:
    - lint
    - build

include:
    - local: '/ci/lint.yaml'
    - local: '/ci/build.yaml'
    # - local: '/ci/deploy.yaml'

# cache jekyll installation
cache:
    paths:
        - vendor/
